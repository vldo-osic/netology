import React, {FC} from 'react'

import '../App.css'
import { IMessage } from '../types/types'

interface TypingProps {
    typing: IMessage
}

const Typing:FC<TypingProps> = ({typing}) => {
    return (
        <div>
              
        </div>
    )
}

export default Typing