
type NewsType = {
    title: string,
    url?: string
    isActive: boolean
    id: number
}

type NewsItem = {
    icon: Icon
    title: string
    url: string
}

type Rate = {
    currency: string
    price: number
    difference: string
}
